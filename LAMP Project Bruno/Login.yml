info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{urlBase}}/api/login"
  body:
    type: json
    data: |-
      {
        "Username": "johnaedo@test.com",
        "Password": "WeLoveJohn!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
