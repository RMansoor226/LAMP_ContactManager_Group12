info:
  name: Get All Contacts
  type: http
  seq: 8

http:
  method: GET
  url: "{{urlBase}}/api/contacts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
